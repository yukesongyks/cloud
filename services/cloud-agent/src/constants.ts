/**
 * Default backend URL for Kilocode API calls when not configured via environment.
 * Used for balance checks, session linking, and other backend API calls.
 */
export const DEFAULT_BACKEND_URL = 'https://api.kilo.ai';
