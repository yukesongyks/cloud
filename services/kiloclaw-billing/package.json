{
  "name": "kiloclaw-billing",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "typecheck": "tsgo --noEmit",
    "lint": "pnpm -w exec oxlint --config .oxlintrc.json services/kiloclaw-billing/src",
    "test": "vitest run"
  },
  "dependencies": {
    "@kilocode/db": "workspace:*",
    "@kilocode/organization-entitlement": "workspace:*",
    "@kilocode/worker-utils": "workspace:*",
    "date-fns": "4.1.0",
    "drizzle-orm": "catalog:",
    "jose": "catalog:",
    "workers-tagged-logger": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
