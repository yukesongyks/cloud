{
  "name": "kiloclaw-inbound-email",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsgo --noEmit",
    "lint": "pnpm -w exec oxlint --config .oxlintrc.json services/kiloclaw-inbound-email/src",
    "cf-typegen": "wrangler types --env-file /dev/null"
  },
  "devDependencies": {
    "@typescript/native-preview": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  },
  "dependencies": {
    "@kilocode/db": "workspace:*",
    "drizzle-orm": "catalog:",
    "node-html-markdown": "2.0.0",
    "postal-mime": "2.7.4"
  }
}
