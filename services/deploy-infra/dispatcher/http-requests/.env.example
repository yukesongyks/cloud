# Copy this file to .env and fill in your credentials
# .env is gitignored so your tokens won't be committed

# Auth tokens for each environment
LOCAL_AUTH_TOKEN=dev-token-change-this-in-production
STAGING_AUTH_TOKEN=your-staging-token-here
PROD_AUTH_TOKEN=your-production-token-here
