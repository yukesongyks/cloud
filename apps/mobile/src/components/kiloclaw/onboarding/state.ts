import { type BotIdentity } from '@/lib/onboarding';

export type { BotIdentity } from '@/lib/onboarding';

export const DEFAULT_BOT_IDENTITY: BotIdentity = {
  botName: 'KiloClaw',
  botNature: 'AI assistant',
  botVibe: 'Helpful, capable, professional',
  botEmoji: '🤖',
};
