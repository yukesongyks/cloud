# scripts

Build-time tooling for the SDK.

`generate-from-schema.ts` will live here. It reads the wasteland schema and
emits `src/commons/schema.generated.ts` and `src/commons/dml.generated.ts`.
The implementation is added by a follow-up task.
